# Build artifacts
/target/

# Cargo lockfile — libraries do not commit Cargo.lock (matches `dig-block` and
# `dig-constants` conventions). Binaries would commit it.
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Repomix context packs (local only)
.repomix/

# GitNexus index (local only)
.gitnexus/

# SocratiCode (Docker-managed, local only)
.socraticode/

# Claude Code local settings & worktrees
.claude/settings.local.json
.claude/worktrees/

# Transient prompt notes (local only)
docs/prompt/chat.md
